import 'package:flutter/material.dart';

class FinanceRegister extends StatelessWidget {
  const FinanceRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
