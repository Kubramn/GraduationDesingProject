import 'package:flutter/material.dart';

class LeaderRequests extends StatelessWidget {
  const LeaderRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
