import 'package:flutter/material.dart';

class LeaderExpenses extends StatelessWidget {
  const LeaderExpenses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
